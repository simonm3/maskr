AS FEW CHANGES AS POSSIBLE TO MASKMM0
=====================================

##### update to pytorch 1
replace data.cpu()[0], item()
replace if x.size(), if x.size()[0]

#### add links
link to maskmm.datasets.nuke for dataset and config (notebooks)
add nuke_train0.ipynb
ln -s <link to maskrcnn trained model>
nms and roialign from maskmm (saves recompiling)

#### fix bugs
makedirs log directory

#### logging/debugging
rename log as log1
add logging
add tracker saves

#### make predictable
set dataloader.num_workers=0, augment=False, shuffle=False

MASKMM
======

#### restructure
structure into multiple files
seperate applications nuke, coco etc..
remove duplication e.g. dataset, bbox_overlaps

#### update
replace numpy with torch where possible
update to pytorch1 removing Variable, .data etc..
add fastai learner

#### align output with original
config.COMPAT for random numbers, dtypes
set dataloader.num_workers=0, augment=False, shuffle=False
GPU STILL HAS DIFFERENCES IN CONV

#### logging/debugging
tracker

#todo detections
#todo demo mold_image still numpy

test on single image
    inputs from dataset/dataload
    targets - rpn, anchors, target masks and boxes
    forward loss
    check pre-step weights, grad
    check post step weights
test more images (as above)
train convergence

##### runs 10% faster on GPU - likely due to numpy=> pytorch
##### similar accuracy. needs longer test run
##### not tried fastai learner

#### working on gpu with fastai learner.
#### callbacks not working for showgraph. num_workers>0 fails
#### fix batch size>1
#### try keras??

